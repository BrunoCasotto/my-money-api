var should = require('should');

describe('Array', function() {
  describe('#indexOf()', function() {

    it('shoud be controller === 2', function() {
      var arr = [1,2]
      arr.length.should.be.exactly(2)
    });

  });
});